
from setuptools import setup, find_packages
setup(
    name = 'br_framework',
    version = '0.0.1',
    packages = ['br_framework'],
    package_dir = {'br_framework': '.'},
    description = '',
    install_requires = [],
)

